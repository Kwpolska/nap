language: c
sudo: false
compiler:
 - clang
 - gcc
script:
 - ./autogen.sh
 - ./configure
 - make
 - make DESTDIR='bin' install
 - $CC -Wall -g -DDEBUG --std=c99 -D_POSIX_C_SOURCE=200809L test-nap.c -lm -o test-nap
 - ./test-nap
